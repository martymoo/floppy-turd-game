
//don't trigger when dialog is running
	if(global.dialogPause == 0){

		with(other){
				var hoverTileX = x;
				var hoverTileY = y;
				oTileFocus.x = hoverTileX;
				oTileFocus.y = hoverTileY;
		}

	}
